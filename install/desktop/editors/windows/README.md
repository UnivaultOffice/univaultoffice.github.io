Appcast JSON files for Desktop Editors updates.

- appcast.json: stable channel
- appcastdev.json: dev channel

Keep version/serviceVersion numeric (digits and dots only). Populate package URLs with GitHub Releases assets from:
https://github.com/UnivaultOffice/DesktopEditors/releases

Dev channel uses tag "dev" in the install URL (see build_tools/make.py).